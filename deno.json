{
  "tasks": {
    "build": "swift build -c release",
    "test": "swift build && deno test -A tests/",
    "test:update": "swift build && deno test -A tests/ -- --update",
    "test:ui": "deno run -A tests/scripts/run_ui_tests.ts",
    "test:ui:update": "deno run -A tests/scripts/run_ui_tests.ts --update"
  }
}
